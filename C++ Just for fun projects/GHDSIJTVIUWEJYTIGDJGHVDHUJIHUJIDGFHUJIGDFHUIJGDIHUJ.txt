#include <iostream>
#include <cstdlib>
#include <ctime>

using namespace std;

string line = string(50, '-');

int main() {
	srand(time(nullptr));
	int random = (rand() % 3) + 1;
	int guess;
	int difficulty;
	string o = string(100, 'o');
	cout << "AAA: ";
	cin >> difficulty;

	for (int i = 0; i < 100;) {
		if (difficulty == 1) {
			int re = rand() % 3;
			int x = rand() % 10;
			int y = rand() % 10;
			cout << "Difficulty selected: " << difficulty << " - " << "Points: " << i << endl;
			cout << line << endl;
			cout << "X: " << x << endl;
			cout << "Y: " << y << endl;
			cout << "X + Y = ?" << endl;
			int sum = x + y;
			cout << "Guess: ";
			cin >> guess;

			if (guess == sum) {
				cout << "woo"  << o << "!" << endl;
				cout << line << endl;
				++i;
			}
			else {
				cout << "boo" << o << "!" << endl;
				cout << line << endl;
				i = 0;
			}
		}
	}
}
